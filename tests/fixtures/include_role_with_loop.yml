---
- hosts: localhost
  connection: local
  tags:
    - play2
  tasks:
    - name: Include role with loop
      include_role:
        name: '{{ item }}'
      loop:
        - fake_role
        - display_some_facts

