---
- hosts: all
  tags:
    - play2
  tasks:
    - name: Looping on roles
      block:
        - include_role:
            name: '{{ item }}'
          loop:
            - fake_role
            - fake_role_2
